extends ../_layout/_docs.pug

block variables
  - var title = 'Glsass - the Sass framework'
  - var description = 'Sass centric framework, customizable and proudly based on Sass Modules. You can also call me Glass 🥃 😉'
  - var slug = 'alert'
  - var parent = 'components'

block docs
  h1 Alert
  p Alerts can be use to display feedback messages or any important content.

  .p-example
    .u-mb-2.c-alert(role="alert") A simple default alert message
    .u-mb-2.c-alert.c-alert--primary(role="alert") A simple primary alert message
    .u-mb-2.c-alert.c-alert--secondary(role="alert") A simple secondary alert message
    .u-mb-2.c-alert.c-alert--success(role="alert") A simple success alert message
    .u-mb-2.c-alert.c-alert--danger(role="alert") A simple danger alert message
    .u-mb-2.c-alert.c-alert--warning(role="alert") A simple warning alert message
    .u-mb-2.c-alert.c-alert--info(role="alert") A simple info alert message
    .u-mb-2.c-alert.c-alert--dark(role="alert") A simple dark alert message
    .u-mb-2.c-alert.c-alert--white(role="alert") A simple white alert message

  pre.code(data-lang='HTML')
    code
      :highlight(lang="html")
        <div class="c-alert" role="alert">A simple default alert message</div>
        <div class="c-alert c-alert--primary" role="alert">A simple primary alert message</div>
        <div class="c-alert c-alert--secondary" role="alert">A simple secondary alert message</div>
        <div class="c-alert c-alert--success" role="alert">A simple success alert message</div>
        <div class="c-alert c-alert--danger" role="alert">A simple danger alert message</div>
        <div class="c-alert c-alert--warning" role="alert">A simple warning alert message</div>
        <div class="c-alert c-alert--info" role="alert">A simple info alert message</div>
        <div class="c-alert c-alert--dark" role="alert">A simple dark alert message</div>
        <div class="c-alert c-alert--white" role="alert">A simple white alert message</div>

  p There is not light variant, because it's considered as the default colors.
    br
    | Also, alerts have a more important #[code margin-bottom] than in this example. It will be reduiced to display the diffrent. states

  h2.u-mt-4(class="u-mt-5@md") Sass references

  h3 Variables

  table.o-table
    thead
      tr
        th(scope=col) Name
        th(scope=col) Type
        th(scope=col) Default value
        th(scope=col) Description
    tbody
      tr
        td #[code $c-alert-border-radius]
        td Number unit
        td #[code $g-border-radius]
        td Border-radius of alerts
      tr
        td #[code $c-alert-margin-bottom]
        td Number unit
        td #[code 1vr]
        td Bottom margin of alerts
      tr
        td #[code $c-alert-padding]
        td Number unit
        td #[code 0.5vr]
        td Internal padding
      tr
        td #[code $c-alert-variants]
        td List
        td Generic theme colors
        td Allow to add colors variants based on existing color name palette
