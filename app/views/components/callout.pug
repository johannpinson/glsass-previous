extends ../_layout/_docs.pug

block docs
  h1 Callout
  p Callouts are similar to alerts, but it allows more content display and wannts to be less flashy.

  .p-example
    .c-callout A simple default callout message
    .c-callout.c-callout--primary A simple primary callout message
    .c-callout.c-callout--secondary A simple secondary callout message
    .c-callout.c-callout--success A simple success callout message
    .c-callout.c-callout--danger A simple danger callout message
    .c-callout.c-callout--warning A simple warning callout message
    .c-callout.c-callout--info A simple info callout message
    .c-callout.c-callout--dark A simple dark callout message
    .c-callout.c-callout--white A simple white callout message
  pre.code(data-lang='HTML')
    code
      :highlight(lang="html")
        <div class="c-callout">A simple default callout message</div>
        <div class="c-callout c-callout--primary">A simple primary callout message</div>
        <div class="c-callout c-callout--secondary">A simple secondary callout message</div>
        <div class="c-callout c-callout--success">A simple success callout message</div>
        <div class="c-callout c-callout--danger">A simple danger callout message</div>
        <div class="c-callout c-callout--warning">A simple warning callout message</div>
        <div class="c-callout c-callout--info">A simple info callout message</div>
        <div class="c-callout c-callout--dark">A simple dark callout message</div>
        <div class="c-callout c-callout--white">A simple white callout message</div>

  h2 Sass references

  h3 Variables

  table.o-table
    thead
      tr
        th(scope=col) Name
        th(scope=col) Type
        th(scope=col) Default value
        th(scope=col) Description
    tbody
      tr
        td #[code $c-callout-border-width]
        td Number unit
        td #[code $g-border-radius]
        td Thickness of the border
      tr
        td #[code $c-callout-border-color]
        td Color
        td #[code $g-border-radius]
        td Color of the border
      tr
        td #[code $c-callout-border-radius]
        td Number unit
        td #[code $g-border-radius]
        td Border-radius of callouts
      tr
        td #[code $c-callout-margin-bottom]
        td Number unit
        td #[code 1vr]
        td Bottom margin of callouts
      tr
        td #[code $c-callout-padding]
        td Number unit
        td #[code 0.5vr]
        td Internal padding
      tr
        td #[code $c-callout-variants]
        td List
        td Generic theme colors
        td Allow to add colors variants based on existing color name palette
